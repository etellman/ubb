root.dir <- '~/Documents/U/ubb/sccc/math146'

data.dir <- paste(root.dir, 'data/bps/PC-Text', sep = '/')
hw7.data.dir <- paste(data.dir, 'ch07', sep = '/')

hw.dir <- paste(root.dir, 'week07/hw', sep = '/')
figures.dir <- paste(hw.dir, 'figures', sep = '/')

setwd(hw7.data.dir)

# exercise 33
(2000 - 831)/2000
2000 - 831

# exercise 35
(45956 - 5029) / 45956
